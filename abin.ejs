                        <!-- <div class="wishlist" onclick="addToWishlist('<%= products[i]._id %>', '<%= products[i].name %>', '<%= products[i].image %>', '<%= products[i].category %>','<%= products[i].price %>')"></div> -->

                        //  async function addToWishlist(productId, name, image, category, price) {
                            //         const userId = "<%= userId %>";
                            
                            //         if (!userId || userId === "null") {
                            //             alert("Please log in to add items to your wishlist.");
                            //             window.location.href = "/login";
                            //             return;
                            //         }
                            
                            //         try {
                            //             const response = await fetch('/add-to-wishlist', {
                            //                 method: 'POST',
                            //                 headers: { 'Content-Type': 'application/json' },
                            //                 body: JSON.stringify({ userId, productId, name, image, category, price })
                            //             });
                            
                            //             const data = await response.json();
                            //             alert(data.message);
                            //         } catch (error) {
                            //             console.error("Error:", error);
                            //             alert("An error occurred while adding to wishlist.");
                            //         }
                            //     }
                            